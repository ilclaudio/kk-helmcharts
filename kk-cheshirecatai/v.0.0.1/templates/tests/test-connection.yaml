apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kk-cheshirecatai.fullname" . }}-test-connection"
  labels:
    {{- include "kk-cheshirecatai.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kk-cheshirecatai.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
